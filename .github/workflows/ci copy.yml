name: First pipeline(2)

on: [push]

jobs:
  build:
  runs-on: ubuntu-latest
  steps:
  - name: Check-out code
    uses: actions/checkout@v2
  - name: we are testing 
    run: echo hello world
    
