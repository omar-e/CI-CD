name: First pipeline(2)

on: [push]

jobs:
  build:
  runs-on: ubunto-latest
  steps:
  - name: Check-out code
    uses: actions/checkout@v2
  - name: we are testing 
    uses: echo hello world
    
